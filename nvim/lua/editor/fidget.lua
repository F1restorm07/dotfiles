-- TODO: integrate with all vim.notify() notifications
require('fidget').setup({
        progress = {
                display = {
                        done_icon = "[*-*]"
                },
        },
})
